<nav class="navbar navbar-expand-lg navbar-light bg-light toroide-navigator">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto toroide-col-center">
            {% for category in categories %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('spiral_front_category', {'categorySlug': category.slug}) }}">{{ category.title }}</a>
                </li>
            {% endfor %}
        </ul>
    </div>
</nav>